using UnityEngine;

public class CurrentDoor : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //FindFirstObjectByType<RoomManager>().currentDoor = gameObject;
    }
}
